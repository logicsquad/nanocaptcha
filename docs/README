Documentation is generated by Webby:

http://webby.rubyforge.org

Webby is a very cool framework for generating static HTML files based on user templates.

To build HTML documentation:

    # webby

This parses the headers, etc., and puts results in the output/ directory.

To work on the documentation locally:

    # webby autobuild

To deploy:

    # webby deploy
